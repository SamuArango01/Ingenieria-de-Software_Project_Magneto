/**
 * Mappers para transformar datos de la API del servidor
 * al formato esperado por el cliente en la feature General
 *
 * Reutiliza la función principal de mapeo de Overview
 */

export { mapCandidateFromApi } from '@/features/overview/mappers/analytics.mapper';
